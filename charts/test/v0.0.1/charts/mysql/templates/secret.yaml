apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  labels:
    app: mysql-secret
type: Opaque
data:
    mysql-root-password: {{ .Values.mysqlRootPassword | b64enc | quote }}
    mysql-password: {{ .Values.mysqlPassword | toString | b64enc | quote }}
    mysql-user: {{ .Values.mysqlUser | b64enc | quote }}